RESTART server.name

Restarts the IRC server.

- Requires Oper Priv: oper:admin

RESTART server.name server.name

Restarts a different IRC server on the network, if the
server is configured to accept remote die/restart commands.

Remotely restarting a server requires the netadmin priv.

- Requires Oper Priv: oper:netadmin
